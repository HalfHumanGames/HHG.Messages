using System.Linq;

namespace HHG.Messages
{
    public static class FetchExtensions
    {
        //public static T Fetch<T>(this IMessage message, object id = null)
        //{
        //    return message.Publish<Locate<T>, T>(id).FirstOrDefault();
        //}

        //public static T[] FetchAll<T>(this IMessage message, object id = null)
        //{
        //    return message.Publish<Locate<T>, T>(id);
        //}
    }
}
